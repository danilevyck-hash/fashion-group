// ─────────────────────────────────────────────────────────────────────────────
// EL INTERRUPTOR DE «UNA SOLA VENTA» (23-sep-2026), en su propio archivo.
//
// Vive solo para que todo lo que lo lee —el módulo puro, las rutas, los Excel y
// las pantallas— vea el MISMO valor, y para que el candado pueda apagarlo de un
// solo lugar y comprobar que las pantallas vuelven a ser las de antes. Ver
// `una-sola-venta.ts` para lo que prende.
//
// `true`  = la misma venta da el MISMO número en Resumen, Clientes › Utilidad y
//           Productos; 2022 y 2023 se sirven; el Excel de Clientes se explica
//           solo; la matriz dice «no vendiste» en vez de «n/a».
// `false` = las pantallas y los archivos de antes del 23-sep-2026.
// ─────────────────────────────────────────────────────────────────────────────

export const UNA_SOLA_VENTA = true;
